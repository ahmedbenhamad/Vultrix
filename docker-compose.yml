version: '3.8'

services:
  qdrant:
    image: ahmedbenhamad/pentest-qdrant:latest
    container_name: qdrant-db
    restart: unless-stopped
    ports:
      - "6333:6333"
      - "6334:6334"
    networks:
      - pentest_net
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:6333/healthz" ]
      interval: 10s
      timeout: 5s
      retries: 5

  pentest-rag:
    build: .
    container_name: pentest-rag
    restart: unless-stopped
    command: [ "python", "main.py", "--serve" ]
    stdin_open: true
    tty: true
    depends_on:
      qdrant:
        condition: service_started
    ports:
      - "8000:8000"
      - "8001:8001"
    environment:
      - QDRANT_URL=http://qdrant
      - OLLAMA_BASE_URL=http://host.docker.internal:11434
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
      - huggingface_cache:/root/.cache/huggingface
    networks:
      - pentest_net

networks:
  pentest_net:
    driver: bridge

volumes:
  huggingface_cache:
