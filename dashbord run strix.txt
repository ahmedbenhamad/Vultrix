# backend  (self-seeds admin@strix.local / ChangeMe123! + demo data)
cd web/backend && python -m venv .venv && .venv/Scripts/activate
pip install -r requirements.txt && cp .env.example .env   # set DATABASE_URL=sqlite+pysqlite:///./strix_console.db
uvicorn app.main:app --reload --port 8000

# frontend
cd web/frontend && npm install && npm run dev   # http://localhost:3000